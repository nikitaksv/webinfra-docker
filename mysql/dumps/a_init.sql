/* Создаем схему для redmine, на случай если она не указана в .env*/
CREATE SCHEMA IF NOT EXISTS `redmine`;
/* Создаем схему для roundcube */
CREATE SCHEMA IF NOT EXISTS `roundcube`;
/* Создаем схему для nextcloud */
CREATE SCHEMA IF NOT EXISTS `nextcloud`;